<div class="row">
  <div class="columns four">
    <h4>Site Sales</h4>
    <ul class="no-bullet">
      {{#each siteSale in controller}}
        <li>{{#linkTo siteSales.show siteSale}}{{siteSale.location}} <span class='label secondary radius'>{{siteSale.siteSaleDate}}</span> {{/linkTo}}</li>
      {{/each}}
    </ul>
    {{#linkTo siteSales.new class="button small radius mbm"}}Add New Site Sale{{/linkTo}}
  </div>
  <div class="columns eight">
    {{outlet}}
  </div>
</div>